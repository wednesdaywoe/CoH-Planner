# 2026-07-06 Tasks

> Relates to: DEDUCTIVE_SCHEMA_HARNESS.md

Execution Order

DSH8 final sweep for Alpha/Genesis + targeted pvMode/resistible follow-up
Scope link: DEDUCTIVE_SCHEMA_HARNESS.md:415
Owner: Incarnate converter + calc maintainer
Primary files: convert-incarnate-effects.cjs, incarnate-effects-completeness.test.ts, dsh8-incarnate-collapse.test.ts, genesis-incarnate.test.ts, incarnate-exemplar.test.ts
Status (2026-07-06, started):
- Completed targeted Parse6/Thunderspy empty-aspect recovery in `convert-incarnate-effects.cjs` for Destiny (`Endurance`/`RechargeTime`/`Recovery`/`Regeneration` + movement fan-in) and Hybrid (`Regeneration`), then regenerated thunderspy incarnates.
- DSH8 detector result: thunderspy class-absent 14 -> 0 (high-confidence collapses stayed 0); homecoming unchanged at 0; rebirth remains non-gating class-absent 12.
- Validation run: `npm run validate:incarnate-collapse` PASS, focused DSH8 tests PASS, `npm run lint` PASS.
Next DSH8 follow-up:
- Triage rebirth residual class-absent bucket (`Movement|run`, `Movement|fly`, `Endurance` on Clarion Radial) as either by-design strength-only amplification or a converter mapping gap, then codify the decision (converter map or detector by-design fold).
Status (2026-07-06, continued):
- Rebirth Clarion residual triage completed and codified in the DSH8 detector as a by-design fold: slot-aware `aspect=Str` filtering now mirrors converter routing (Destiny keeps only `RechargeTime`/`Recovery`/`Regeneration` at Strength; Hybrid keeps only `DamageBuff`/`Accuracy`/`EnduranceDiscount`).
- DSH8 detector result now: class-absent 0 on all datasets (homecoming/rebirth/thunderspy), high-confidence collapses remain 0.
- Validation rerun: `node scripts/dsh8-incarnate-collapse-detector.cjs --dataset rebirth --top 20` clean; `npm run validate:incarnate-collapse` PASS; DSH8 regression tests PASS; `npm run lint` PASS.
Run commands:
npm run lint
npm run validate:incarnate-collapse
npm test -- incarnate-effects-completeness.test.ts dsh8-incarnate-collapse.test.ts genesis-incarnate.test.ts incarnate-exemplar.test.ts
Pass criteria:
No new class-present sibling-missing findings in DSH8 gate on all datasets.
Alpha/Genesis calc outputs stay stable except for intentional fixes.
No regressions in existing incarnate completeness assertions.

DSH9 bridge convergence (eliminate parallel attrib bridges)
Scope link: DEDUCTIVE_SCHEMA_HARNESS.md:469
Owner: Enhancement extractor maintainer
Primary files: extract-rebirth-io-sets-v2.py, extract-proc-data.py, atomic-effect.ts, tools/mids-oracle/diff_enh_oracle.py
Status: Completed
- Added shared DSH4 bridge adapter CLI at `scripts/bridge-attrib-one.cjs` (Node + `bridgeAttrib` from `src/data/core/atomic-effect.ts`).
- `extract-rebirth-io-sets-v2.py` now resolves set-bonus stat keys via bridge-first `_bonus_stat_from_bridge(...)`, with narrow legacy fallbacks only for known non-bridge edge cases (`healing_strength`, `+res(slow)`, `+res(recharge_debuff)`).
- `_resolve_bonus_effects` now keeps template `table` context per `(attrib, aspect, table)` so bridge disambiguation can use table where relevant.
- `extract-proc-data.py` single-attrib mapping in `_group_effects` now routes through bridge-first `_proc_effect_from_bridge(...)`, preserving explicit fallback for slow/recharge-resistance proc categories.
- Validation run: `python3 tools/mids-oracle/test_read_enhdb.py` PASS, `python3 tools/mids-oracle/test_diff_enh_oracle.py` PASS, `npm test -- src/data/io-sets-bonus-keys.test.ts src/data/set-bonus-groups.test.ts` PASS, `npm run lint` PASS.
- Baseline gate verification: `/Users/brian/github/CoH-Sidekick/.venv/bin/python tools/mids-oracle/diff_enh_oracle.py --dataset homecoming --baseline tools/mids-oracle/enh_oracle_residual_baseline.json --strict` PASS (`new signatures vs baseline = 0`).
Run commands:
python3 test_read_enhdb.py
python3 test_diff_enh_oracle.py
npm test -- io-sets-bonus-keys.test.ts set-bonus-groups.test.ts
Pass criteria:
No functional drift in mapped stat keys after bridge unification.
Enh oracle residual profile does not worsen versus current frozen baseline.
Set-bonus key/group tests remain green.

DSH9 identity-keyed set-bonus family grouping + mez-resist/drop fixes
Scope link: DEDUCTIVE_SCHEMA_HARNESS.md:473
Owner: IO-set extraction + set-bonus calc maintainer
Primary files: extract-rebirth-io-sets-v2.py, set-bonuses.ts, io-sets-bonus-keys.test.ts, set-bonus-groups.test.ts, mez-duration-bonus.test.ts
Status: Completed
- Reworked `extract-rebirth-io-sets-v2.py` `_resolve_bonus_effects` from value-keyed grouping to identity-keyed aggregation (`stat` identity first, representative value selected per key), eliminating float-split family leakage.
- Family collapse (damage buff / all-resistance / all-mez-resistance) now happens by identity regardless of minor value drift, then remaining entries route through bridge-based stat resolution.
- Fixed single-type mez-resistance drop by mapping bridge `MezResist` to planner aggregate key `mez_resistance_(all)` (planner tracks mez resistance as one aggregate stat).
- Kept paired-stat de-dupe behavior (`_BONUS_STAT_PAIRS`) after identity aggregation to avoid lockstep double-application in calc pairing.
- Validation run: `npm run lint` PASS; `npm test -- src/data/io-sets-bonus-keys.test.ts src/data/set-bonus-groups.test.ts src/utils/calculations/mez-duration-bonus.test.ts` PASS; `python3 -m py_compile scripts/extract-rebirth-io-sets-v2.py scripts/extract-proc-data.py` PASS.
Run commands:
npm run lint
npm test -- io-sets-bonus-keys.test.ts set-bonus-groups.test.ts mez-duration-bonus.test.ts
Pass criteria:
Family grouping is identity-based (not float-value keyed) with no split/merge artifacts.
Single-type mez-resist no longer dropped.
No naming lockstep mismatch between extractor outputs and set-bonus stat map.

Session documentation (2026-07-06, DSH9 closeout)
- Confirmed DSH9 bridge convergence implementation is bridge-first in both extractors via shared adapter `scripts/bridge-attrib-one.cjs`.
- Ran the frozen-baseline gate explicitly:
	- `/Users/brian/github/CoH-Sidekick/.venv/bin/python tools/mids-oracle/diff_enh_oracle.py --dataset homecoming --baseline tools/mids-oracle/enh_oracle_residual_baseline.json --strict`
	- Result: PASS, `new signatures vs baseline = 0`.
- Re-ran DSH9 identity-keyed validation bundle:
	- `npm test -- src/data/io-sets-bonus-keys.test.ts src/data/set-bonus-groups.test.ts src/utils/calculations/mez-duration-bonus.test.ts`
	- `/Users/brian/github/CoH-Sidekick/.venv/bin/python -m py_compile scripts/extract-rebirth-io-sets-v2.py scripts/extract-proc-data.py`
	- `npm run lint`
	- Result: PASS (3/3 test files, 19/19 tests; py_compile clean; tsc clean).
- Documentation corrections made in this stream:
	- Marked both DSH9 items as `Status: Completed`.
	- Corrected DSH9 primary-file path from `diff_enh_oracle.py` to `tools/mids-oracle/diff_enh_oracle.py`.

DSH9 proc allowlist gap closure in runtime application path
Scope link: DEDUCTIVE_SCHEMA_HARNESS.md:478
Owner: Proc extraction + calc runtime maintainer
Primary files: character-totals.ts, proc-data.ts, ato-passive-globals.test.ts, proc-effects-coverage.test.ts, io-sets-proc-resolution.test.ts, stealth-procs.test.ts
Status: Completed
Notes:
- Runtime proc application now handles `Absorb` explicitly and records `proc` breakdown sources under `absorb`.
- `GlobalBonuses` now carries typed `absorb` initialization to keep runtime writes/schema aligned.
- Proc slot collection now includes a guarded legacy fallback for `isProc:false` slots when the slot name still clearly matches proc identity (exact/contains ioName or known placeholder names), closing a silent-drop class without broad false positives.
- Proc lookup (`findProcData`) now preserves set specificity when an exact bare-name key belongs to a different set (fixes shared-name collisions like `Chance for +Absorb`).
- Added regression coverage in `src/utils/calculations/proc-runtime-allowlist.test.ts` for defense-global application, absorb global application, and legacy `isProc:false` acceptance path.
Run commands:
npm run lint
npm test -- ato-passive-globals.test.ts proc-effects-coverage.test.ts io-sets-proc-resolution.test.ts stealth-procs.test.ts
Pass criteria:
No silent default drop in proc application path for valid mapped effects.
Typed Defense proc handling works beyond only all-typed cases when intended.
proc:false passive-global class is either intentionally handled or explicitly excluded with test-covered rationale.
Validation:
- `npm test -- src/data/ato-passive-globals.test.ts src/data/proc-effects-coverage.test.ts src/data/io-sets-proc-resolution.test.ts src/utils/calculations/stealth-procs.test.ts src/utils/calculations/proc-runtime-allowlist.test.ts` PASS (24/24).
- `npm run lint` PASS.

Replace AT-table extractor allowlist with principled filter
Scope link: DEDUCTIVE_SCHEMA_HARNESS.md:487
Owner: Converter platform/data pipeline maintainer
Primary files: extract-at-tables.cjs, converter-table-integrity.test.ts, at-tables.ts, validate-converter-output.cjs
Status: Completed
Notes:

- Removed `extract-at-tables.cjs` hand-maintained `RELEVANT_TABLES` allowlist and replaced it with a source-driven filter that accepts every usable `named_tables` entry present on player AT + pet class exports (numeric, finite arrays only), keyed case-insensitively.
- This keeps extraction correctness tied to binary source data shape instead of static table-name maintenance, eliminating the silent-omission class that DSH2 surfaced.

Run commands:
npm run lint
npm test -- converter-table-integrity.test.ts
npm run validate:converter
Pass criteria:
No hand-maintained fixed list needed for correctness.
All player-referenced fatal-slot AT tables resolve.
Converter validation remains green across homecoming/rebirth/thunderspy.
Validation:

- `npm run lint` PASS.
- `npm test -- converter-table-integrity.test.ts` PASS (4/4).
- `npm run validate:converter` PASS on homecoming/rebirth/thunderspy (all gate checks clean).

Not In Execution Path
DSH7 is intentionally descoped and should stay documentation-only unless a specific numeric dispute appears:
