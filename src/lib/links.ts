/**
 * External links — single source of truth.
 *
 * Edit a URL here and every button/link in the app updates. Imported wherever
 * an external destination is referenced (Discord, etc.) so there are no
 * hard-coded URLs scattered across components.
 */

/** Community Discord server invite (used by the page-footer button, mobile nav, welcome + feedback modals). */
export const DISCORD_INVITE_URL = 'https://discord.gg/5HmYsACBv6';

/** Direct-message channel for feedback — a personal DM, NOT the server invite. */
export const DISCORD_FEEDBACK_DM_URL = 'https://discord.com/channels/@me/570068130320220172';
