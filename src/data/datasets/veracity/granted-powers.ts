/**
 * Veracity granted-power groups — re-exports HC's mapping wholesale.
 *
 * Veracity uses the standard 14 archetypes with no custom form-shifter AT, so
 * the HC auto-grant model (Adaptation stances, Dual Pistols ammo swap, Fly →
 * Afterburner, Kheldian Nova/Dwarf forms, etc.) applies unchanged. Replace with
 * a Veracity-specific map only if the server adds custom granted-power behavior.
 */

export { GRANTED_POWER_GROUPS } from '../homecoming/granted-powers';
