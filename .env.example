# Copy to .env (gitignored) and fill in per-machine secrets.

# Discord incoming webhook for changelog announcements.
# Create at: Discord → Server Settings → Integrations → Webhooks → New Webhook,
# pick the target channel, then "Copy Webhook URL".
DISCORD_CHANGELOG_WEBHOOK_URL=
